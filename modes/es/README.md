# modes/es — Spanish locale pack (opt-in)

Enable by setting `locale: es` in `config/profile.yml`.

Files here mirror modes/*.md but in Spanish.
Currently stubbed — add `modes/es/gig.md`, `modes/es/proposal.md`, etc. to complete the pack.

